using System;

namespace Зачёт
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Числа Раффа");
            double S, N, k
            int[] myArr = new int[S] { 2, 5, k };
            for (var k = 0u; k < n; k++)
            {
                if (IsPrimeNumber(k))
                {
                    Console.Write($"{k} ");
                }
                if (num == 0)
                {
                    break;
                }
                if ((num % 10) && (num = 7))
                {
                }
            }
            {
                int N = 44030;
                N = 2 * 5 * k;
            }
            Console.WriteLine();
        }
    }
}
